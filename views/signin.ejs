<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>File Uploader | Sign-in to continue</title>
</head>

<body>
  <header>
    <button><a href="/login">Login</a></button>
    <button><a href="/sign-in">Sign In</a></button>
  </header>
  <h1>Sign In</h1>
  <% if(locals.errorMessage) {%>
  <h2><%= errorMessage %></h2>
  <% } %>
  <form action="sign-in" method="post">
    <label for="name">Name: </label>
    <input type="text" name="name" id="name" value="<%= (locals.previousInputs)? previousInputs.name : '' %>" />
    <label for="email">Email: </label>
    <input type="email" name="email" id="email" value="<%= (locals.previousInputs)? previousInputs.email : '' %>" />
    <label for="password">Password</label>
    <input type="password" name="password" id="password" />
    <button type="submit">Sign In</button>
  </form>
</body>

</html>